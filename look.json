{
  "_comment": [
    "The look, in one place. Every stage reads this; nothing hardcodes a look value any more.",
    "The Grade Bench (bench/) writes this file when you send a grade, so the browser is the",
    "settings app and the scripts stay dumb. Edit by hand if you prefer — it is just numbers.",
    "",
    "tone.* are the arguments to scripts/make-tone-lut.py. luts/tone/shipped.cube is GENERATED",
    "from them: 02-grade.sh regenerates it whenever this file is newer, so the .cube can never",
    "silently disagree with the numbers recorded here.",
    "",
    "Provenance of the current values: graded by eye in the Bench against the RAL references in",
    "frame, then adjusted once for shadow detail (black 0.015 -> 0.025, gamma 2.09 -> 2.02).",
    "saturation 1.27 is DELIBERATELY off-spec — it puts the traffic blue at B/G 2.39 against a",
    "1.98 spec. That is the grade, not an error. See docs/adr/0001_GRADE_BY_TONE_NOT_BY_COLOUR.md.",
    "",
    "match.reference_yavg is the post-CST luma mean (10-bit) the look was tuned against, measured",
    "on IMG_0609. grade.sh solves each clip's gamma to land on it, so that every clip in a shoot",
    "window gets the same look rather than the same curve. It lives here because it is a",
    "measurement of the grade, not a knob: change it only if the look is re-tuned on another frame."
  ],
  "tone": {
    "gamma": 2.02,
    "pivot": 0.39,
    "contrast": 1.09,
    "toe": 0.0,
    "shoulder": 0.1,
    "black": 0.025
  },
  "colour": {
    "saturation": 1.27,
    "warmth": 0.005
  },
  "grain": {
    "strength": 8
  },
  "stabilisation": {
    "smoothing": 30
  },
  "match": {
    "reference_yavg": 609
  }
}
